# Explainable Intervention Prioritization System (EIPS)
# Python 3.11+

# Core data processing
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
openpyxl==3.1.5
xlrd==2.0.1

# Machine learning
scikit-learn==1.5.2
xgboost==2.1.3
optuna==4.1.0
imbalanced-learn==0.13.0

# Explainable AI
shap==0.46.0

# NOTE: tensorflow (used only by the optional LSTM/GRU sequence-model
# training script, src/training/sequence_model.py -- a separate synthetic
# demonstration track, never imported by app.py or any Streamlit page) is
# deliberately left out of this file. It has no prebuilt wheel for newer
# Python versions, which breaks the hosted deploy; install it locally with
# `pip install tensorflow==2.18.0` only if you need to run that training
# script yourself.

# Web application
streamlit==1.40.2

# Visualization
plotly==5.24.1
matplotlib==3.9.3

# Database
sqlalchemy==2.0.36
# Postgres driver -- only used when DATABASE_URL points at Postgres (see
# config/settings.py); SQLite (the local-dev default) needs no driver here.
psycopg2-binary==2.9.10

# Authentication
bcrypt==5.0.0

# Model persistence
joblib==1.4.2

# Configuration
pyyaml==6.0.2
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-cov==6.0.0
